// next.config.js
/** @type {import('next').NextConfig} */ // Keep this JSDoc for IDE type hinting
const nextConfig = {
    // Your Next.js configuration options here
    // e.g., output: 'standalone', images: { domains: ['res.cloudinary.com'] }
}

module.exports = nextConfig // <--- Standard CommonJS export
